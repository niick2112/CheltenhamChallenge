{
  "name": "cc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:css && npm run build:js && npm run build:pages",
    "build:css": "copyfiles src/index.css -f docs",
    "build:js": "copyfiles src/index.js -f docs",
    "build:pages": "copyfiles src/index.html -f docs"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "stylus": "latest",
    "copyfiles": "latest"
  }
}
