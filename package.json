{
  "name": "cc",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm run build:css && npm run build:js && npm run build:pages",
    "build:css": "stylus src/index.css > docs/main.css",
    "build:js": "browserify src/index.js > docs/index.js",
    "build:pages": "copyfiles src/index.html -f docs",
    "watch": "watchify index.js > docs/index.js"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "stylus": "latest",
    "browserify": "latest",
    "watchify": "latest"
  }
}
